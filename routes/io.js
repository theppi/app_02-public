/**
 * This module maps keywords, that should be used in the websocket message
 * to the function, that is triggered by this message.
 */

module.exports = {
    test: function(socket, msg) {

    }
}